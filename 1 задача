//Задано масив із n цілих чисел. Виведіть лише його позитивні елементи,
//не змінюючи початковий порядок.
//Вхідні дані: Перший рядок містить число n (1 ≤ n ≤ 100). У другому рядку
//записані n цілих чисел, кожне з яких не перевищує 100 за модулем. 
//Вихідні дані: У першому рядку виведіть кількість позитивних елементів
//масиву. У другому рядку виведіть самі позитивні елементи. Якщо позитивних елементів у масиві немає, виведіть "NO".

#include <stdio.h>
int main()
{
    int m, positive = 0;
    do
    {
        printf("Ведіть кільскість: ");
        scanf("%i", &m);
    }
    while ( m < 1 || m > 100);
    int N[m];
    for (int i = 0; i < m; i++)
    {
        scanf("%i", &N[i]);
        if ( N[i] > 0)
        {
            positive++;
        }
    }
    printf("%i\m", positive);
    if (positive!=0){
        for (int i = 0; i < m; i++)
        {
            if ( N[i] > 0)
            {
                printf("%i ", N[i]);
            }
        }
    }
    else printf("NO");
}
